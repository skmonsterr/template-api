{
  "name": "template-api",
  "version": "1.0.0",
  "description": "template de uma aplicacao api",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist && npx tsc",
    "dev": "ts-node-dev -r tsconfig-paths/register --inspect --transpile-only --ignore-watch node_modules src/shared/http/server.ts"
  },
  "author": "joao martins",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.8.3",
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.1.0",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "rimraf": "^3.0.2"
  }
}
